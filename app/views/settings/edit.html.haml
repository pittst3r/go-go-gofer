%h1 Your Settings

= form_for current_user, url: settings_path, method: :put do |f|
  %h4= f.label :name
  %p= f.text_field :name
  %h4= f.label :email
  %p= f.email_field :email
  %p= submit_tag "Update"
